#include "main.h"
/**
 * positive_or_negative - checks if the number is positive or negtive
 *
 * @i : the number to be checked
 *
 */
void positive_or_negative(int i)
{
	if (i < 0)
		printf("%d is negetive\n", i);
	else
		printf("%d is positive\n", i);
}
